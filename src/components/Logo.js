const Logo = ({ className = '' }) => (
  <span
    className={`font-serif text-lg font-bold tracking-tight text-accent group-hover:text-ink transition-colors ${className}`}
  >
    JN
  </span>
);

export default Logo;
